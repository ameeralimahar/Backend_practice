<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Practice Project</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="/javascripts/script.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 ">
        <div class="form p-10 text-white">
            <form action="/create" method="post" >
                <input class="block px-5 py-2 bg-zinc-800 w-full rounded-lg" type="text" placeholder="Title" name="title">
                <textarea class="block mt-2 px-5 py-2 bg-zinc-800 resize-none w-full rounded-lg" name="details" id="" placeholder="Write task details"></textarea>
                <input class="mt-2 bg-blue-600 px-2 py-2 rounded-md" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap">
            <% if(files.length >0) { %>
                <% files.forEach(function(value){ %>
                    <div class="task  min-w-72 px-3 py-4 bg-zinc-800 rounded-md">
                        <h1 class=" text-white text-3xl tracking-tighter"><%= value %></h1>
                        <div class="w-full flex justify-between items-center mt-3">
                            <a class="text-blue-500 inline-block" href="/files/<%= value%>">Read More.</a>
                            <a class="text-zinc-600" href="/edit/<%= value%>">edit filename</a>
                        </div>
                    </div>
            <%    }) %>
            <% } else { %>
            <h3 class="text-zinc-600">No task yet.</h3>

            <% } %>

            
            
        </div>
    </div>
    
</body>
</html>
